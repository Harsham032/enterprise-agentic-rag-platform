# Runtime dependencies for the retrieval platform.
# Optional backends (PostgreSQL, Qdrant, Redis, transformer models) live in
# pyproject.toml extras so the default install stays small and offline-capable.
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
pydantic>=2.6
pydantic-settings>=2.2
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
PyYAML>=6.0
python-dotenv>=1.0
structlog>=24.1
rank-bm25>=0.2.2
lxml>=5.1
beautifulsoup4>=4.12
pypdf>=4.1
SQLAlchemy>=2.0
prometheus-client>=0.20
tenacity>=8.2
